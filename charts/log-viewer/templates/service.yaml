apiVersion: v1
kind: Service
metadata:
  name: {{ include "log-viewer.fullname" . }}
  labels:
    {{- include "log-viewer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "log-viewer.selectorLabels" . | nindent 4 }}
